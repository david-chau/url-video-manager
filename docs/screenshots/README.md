# Screenshots

Referenced from the top-level README. Keep these names — the README links to them directly:

- `add-form.png` — the add panel: URL box, download options, subtitle generation controls
- `jobs-table.png` — the queue: playlist parent with per-item rows, progress, per-job actions

Retake at a wide viewport (~1000px content width) so the table doesn't collapse
into its narrow-screen card layout, which is a different thing to show.
